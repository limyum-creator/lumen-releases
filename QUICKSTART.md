# 光栈 3 分钟上手指南

欢迎使用光栈（Lumen）。本文将带你在几分钟内完成图库初始化、素材归档与第一次画布推演。

---

## 1. 启动与图库初始化

1. **安装启动**：打开 DMG，将 `光栈.app` 拖入 `应用程序`。首次打开若遇系统安全拦截，在「系统设置 → 隐私与安全性」中点击「仍要打开」。
2. **选择图库**：
   - **首次使用**：点击「新建图库」，选一个**专用的空文件夹**（例如 `~/Pictures/LumenLibrary`）。
   - **已有图库**：点击「打开已有图库」，选中包含 `.lumen` 的目录即可。
   > 📌 **贴士**：图库是光栈存放素材与创作记录的总目录，建议使用单独新建的文件夹，避免直接将杂乱的系统目录设为图库。

---

## 2. 导入与浏览素材

1. **智能监听（最省心）**：
   - 进入「设置 → 图库与连接」，添加你常用的下载目录或出图目录作为**监听文件夹**；
   - 光栈会自动将新增素材收进图库，并保留原始来源。
2. **看图操作秘诀**：
   - **空格键**：快速全屏预览（支持图片与音视频流畅播放）；
   - **双击**：进入作品详情，查看该图当次的 Prompt、Seed、模型参数，或并排比对历史版本；
   - **Command + 点击**：追加多选；**Shift + 点击**：连续多选；
   - **爱心**：收藏灵感，后续可通过筛选栏一键定位。

---

## 3. 开始你的第一个创作项目

1. **进入「创作」工作区**：
   - 新建一个项目（例如 *赛博朋克短片概念设计*）；
   - 根据创作层次，建立单集与单元（单元可以是一个镜头组、一个场景或一组并行的视觉探索）。
2. **在无限画布上构思**：
   - **添加卡片**：在画布空白处右键，可放置「素材卡」、「提示词卡」、「便签」或「产出卡」；
   - **自由编排**：拖动画布、框选多张卡片移动、缩放调整视觉焦点；
   - **连线串联**：把左侧的参考图或提示词拖出连线接在产出卡上，直观理清生成逻辑。

---

## 4. 连接生成引擎（按需启用）

光栈是一个开放工作台，你可以纯离线整理素材，也可以连上引擎直接推演：

### 选项 A：连接本地 ComfyUI
- 在「设置」中填入你正在运行的 ComfyUI 服务地址（如 `http://127.0.0.1:8188`）并测试连接；
- 在产出卡上直接选择对应的 ComfyUI 工作流、映射输入参数并提交。

### 选项 B：连接云端 RunningHub
- 在「设置 → 连接 → RunningHub」中配置你的 API Key（安全存储于 Mac 本地钥匙串）；
- 粘贴 RunningHub 工作流链接或 ID，读取后即可在产出卡上调配参数并直接出图，结果会自动下载解压并回收入库。

---

## 5. 常用技巧与数据安全

- **版本定版**：同一张产出卡多次生成时，默认跟随最新结果。如果某一版特别满意，点击「固定此版本」锁定展示。
- **备份建议**：如需迁移或备份，完整打包你的图库目录即可（**务必包含隐藏文件夹 `.lumen`**，这里保存着版本关系与项目元数据）。
- **与 AI 助手协同**：在「设置 → Agent 连接」导出配置文档，配合 OpenClaw 或 Claude MCP，AI 助手就能帮你理清分镜和批量改词。

遇到疑问？随时来 [GitHub Issues](https://github.com/limyum-creator/lumen-releases/issues) 提问或参与讨论！
